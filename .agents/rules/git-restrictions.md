---
trigger: always_on
---

# Git & PR Restriction Policy

- NEVER execute `git push` without explicit confirmation from the user.
- NEVER execute `gh pr create` or `gh pr merge` without explicit confirmation from the user.
- All work must remain committed locally only.
