{
  "name": "connectsphere-spm",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently -n \"server,client\" -c \"blue,green\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server",
    "test": "npm run test --workspace server && npm run test --workspace client && npm run test:reviewer",
    "test:regression:sg2-42": "npm exec --workspace server -- tsx --test src/venues.test.ts src/db/venues.test.ts src/logout.test.ts src/authorization.test.ts && npm exec --workspace client -- vitest run src/venues/Venues.test.tsx src/App.test.tsx",
    "ci": "npm run build && npm run test:coverage",
    "test:e2e": "tsc -p e2e/tsconfig.json && playwright test",
    "test:regression": "npm run build && npm run test:e2e",
    "ci:full": "npm run ci && npm run test:e2e",
    "test:reviewer": "node --test .github/scripts/security-review.test.mjs",
    "test:coverage": "npm run test:coverage --workspace server && npm run test:coverage --workspace client && npm run test:reviewer"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "concurrently": "^9.1.2"
  }
}
